# Telegram Bot Framework
python-telegram-bot[ext]==21.6

# Async / utility
python-dotenv==1.0.1
aiofiles==24.1.0
aiohttp==3.10.10
httpx==0.27.2

# YouTube & generic media extraction (keep latest; YouTube breaks often)
yt-dlp

# Instagram
instaloader==4.13.1

# Spotify (metadata only; audio sourced via yt-dlp)
spotipy==2.24.0

# Music recognition (Shazam)
shazamio

# Audio / image processing (metadata, album art, mp3 tags)
mutagen==1.47.0
Pillow==10.4.0

# ffmpeg-python wrapper (you must also install ffmpeg binary on system PATH)
ffmpeg-python==0.2.0
